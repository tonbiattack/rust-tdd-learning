# DESIGN

## 方針

この教材では、原典のAPIをRustへ逐語的に移すのではなく、TDDで学ぶ設計上の問いをRustの標準機能で再表現します。依存関係を増やさず、`cargo test`一回で全テストが実行できることを優先しました。

| 設計課題 | Rustでの選択 | 理由 |
|---|---|---|
| 文字列と反復 | `&str`入力と`String`返却、`repeat` | 借用した入力を所有する出力へ変換する境界を学べる |
| 空の集計結果 | `Option<f64>` | 値が存在しない状態を`None`で表し、例外に依存しない |
| 不変条件 | `Score::new`の`Result` | 生成時に0〜100の範囲を保証し、不正値を型の外へ出さない |
| 抽象化とDI | `Notifier` trait | 実I/Oを持ち込まず、手書きSpyで観測可能にする |
| 状態変更 | `&mut self`と`Vec<Todo>` | 可変性をメソッド境界へ限定し、失敗時の状態を検証する |
| 統合テスト | `tests/todo_workflow.rs` | 公開APIを利用する利用者視点のテストを分離する |

## TDDサイクル

各章のガイドには、最初に追加するテスト、Redで観測すること、Greenの最小方針、Refactorの観点、次の振る舞いを記録しています。完成版ではすべてのテストが通りますが、学習者は一度実装を消してサイクルを再現できます。

## 範囲外

HTTPサーバー、非同期ランタイム、マクロ作成、外部データベースはこの初版の対象外です。これらは標準ライブラリ中心の6章を終えた後に追加する発展課題として扱います。
